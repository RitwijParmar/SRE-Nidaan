# ── SRE-Nidaan Orchestration ──
version: '3.8'

services:
  face:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8001
    depends_on:
      - body
    networks:
      - sre-network

  body:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8001:8001"
    environment:
      - VLLM_ENDPOINT=http://brain:8000/v1
      - MODEL_ID=${MODEL_ID:-meta-llama/Meta-Llama-3-8B-Instruct}
      - GROUNDING_KB_PATH=/app/ops/knowledge_base.json
      - FEEDBACK_LOG_PATH=/app/feedback/analyst_feedback.jsonl
      - PRODUCTION_ARTIFACT_LABEL=${PRODUCTION_ARTIFACT_LABEL:-checkpoint-1064}
      - GENERATION_CANDIDATES=${GENERATION_CANDIDATES:-3}
    volumes:
      - ./feedback:/app/feedback
    depends_on:
      - brain
    networks:
      - sre-network

  brain:
    image: vllm/vllm-openai:latest
    ports:
      - "8000:8000"
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - MODEL_ID=${MODEL_ID:-meta-llama/Meta-Llama-3-8B-Instruct}
      - NEXUS_LORA_PATH=/models/production_adapter
    command: >-
      --model ${MODEL_ID:-meta-llama/Meta-Llama-3-8B-Instruct}
      --enable-lora
      --max-lora-rank 64
      --lora-modules sre-lora=/models/production_adapter
      --max-model-len 2048
      --gpu-memory-utilization 0.90
    volumes:
      - ./results/production_adapter:/models/production_adapter:ro
      - ~/.cache/huggingface:/root/.cache/huggingface
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    networks:
      - sre-network

networks:
  sre-network:
    driver: bridge
